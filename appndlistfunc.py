def append_element(some_list, element): #defining function with list name and new value as arguments
    some_list.append(element) # use the arguments in the right synthx in order to appent the new value to the list.

data = [1,2,3] #initialising list named data
append_element(data, 4) #function call
print(data) #printing the appended list.